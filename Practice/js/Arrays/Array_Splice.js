//.splice() methods splits and return the splitted array
// syntax//
// array.splice(startIndex, Num. of Item to remove, optionally items to add)

const Nums = [1,2,3,4,5,6,7,8,9,0]

let spliceNum = Nums.splice(3, 5)
console.log(spliceNum);
console.log(noSpliceNum);